# Routing priority (source of truth)

Do not duplicate routing priority rules here; they are version-sensitive.

**Source of truth**:
- `${OPENCLAW_REPO:-~/repo/apps/openclaw}/docs/channels/channel-routing.md`

When in doubt:
1) Read the doc above
2) If behavior still mismatches, grep the repo for the resolver used by your version.
