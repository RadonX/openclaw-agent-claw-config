[project]
name = "telegram-kit"
version = "0.1.0"
description = "Telegram provisioning kit (MTProto user API + Bot API), for OpenClaw agent workspace usage."
requires-python = ">=3.10"
dependencies = [
  "telethon>=1.42.0",
  "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
